name: Collaborative Workflow

on:
  push:
    branches:
      - main

jobs:
  update-html_and_readme:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3